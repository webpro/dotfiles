"$schema" = 'https://starship.rs/config-schema.json'

# ============================================================
# PROMPT FORMAT
# ============================================================
format = """
[╭─](fg:blue)\
${custom.machine}\
[](bg:yellow fg:blue)\
$directory\
[](bg:maroon fg:yellow)\
$git_branch\
[](bg:teal fg:maroon)\
$git_status\
[](bg:mauve fg:teal)\
$cmd_duration\
[](bg:flamingo fg:mauve)\
$time\
[](fg:flamingo)
$character\
"""

palette = 'catppuccin_mocha'

# ============================================================
# ACTIVE MODULES (in order of appearance)
# ============================================================

[username]
disabled = true

[custom.machine]
command = "hostname -s | tr '[:lower:]' '[:upper:]'"
when = "true"
style = "bg:blue fg:plop"
format = '[ $output ]($style)'

[directory]
style = "bg:yellow fg:plop"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"
read_only = " 󰌾"

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = "󰇚 "
"Music" = "󰝚 "
"Pictures" = " "
"Programming" = "󰲋 "

[git_branch]
symbol = ""
style = "bg:maroon fg:plop"
format = '([ $symbol$branch ]($style))'

[git_status]
style = "bg:teal fg:plop"
format = '([ $all_status$ahead_behind ]($style))'
conflicted = "✗ "
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕ "
untracked = "? "
stashed = "󰏖 "
modified = "! "
staged = "+ "
renamed = "» "
deleted = "✘ "

[cmd_duration]
show_milliseconds = false
# Start with the connector so it only appears when duration is shown
format = '([ took $duration ]($style))'
style = "bg:mauve fg:plop"
disabled = false
min_time = 2000
show_notifications = true
min_time_to_notify = 45000

[time]
disabled = false
time_format = "%H:%M"
format = '[ ⏳︎$time ]($style)'
style = "bg:flamingo fg:plop"

[character]
success_symbol = '[╰─➜](bold fg:green)'
error_symbol = '[╰─➜](bold fg:red)'
vimcmd_symbol = '[╰─⬅](bold fg:green)'
vimcmd_replace_one_symbol = '[╰─⬅](bold fg:mauve)'
vimcmd_replace_symbol = '[╰─⬅](bold fg:mauve)'
vimcmd_visual_symbol = '[╰─⬅](bold fg:yellow)'

# ============================================================
# COLOR PALETTE (Catppuccin Mocha)
# ============================================================

[palettes.catppuccin_mocha]
rosewater = "#f5e0dc"
flamingo = "#f2cdcd"
pink = "#f5c2e7"
mauve = "#cba6f7"
red = "#f38ba8"
maroon = "#eba0ac"
peach = "#fab387"
yellow = "#f9e2af"
green = "#a6e3a1"
teal = "#94e2d5"
sky = "#89dceb"
sapphire = "#74c7ec"
blue = "#89b4fa"
lavender = "#b4befe"
text = "#cdd6f4"
subtext1 = "#bac2de"
subtext0 = "#a6adc8"
overlay2 = "#9399b2"
overlay1 = "#7f849c"
overlay0 = "#6c7086"
surface2 = "#585b70"
surface1 = "#45475a"
surface0 = "#313244"
base = "#1e1e2e"
mantle = "#181825"
crust = "#11111b"
plop = "#60574e"

# ============================================================
# NOTES & REFERENCES
# ============================================================

# DOCUMENTATION
# - Starship config docs: https://starship.rs/config/
# - Available modules: https://starship.rs/config/#prompt
# - Nerd Font symbols: https://www.nerdfonts.com/cheat-sheet
# - Catppuccin palette: https://github.com/catppuccin/catppuccin


# Currently used symbols:
#   arrow_right = 
#   arrow_left = 
#   semi-circle_decorators_right = 
#   semi-circle_decorator_left = 
#   top_line_connector = ╭─
#   bottom_line_connector = ╰─
#   branch = 
#   read_only = 󰌾
#   untracked = ?
#   stashed = 󰏖
#   modified = !
#   staged = +
#   renamed = »
#   deleted = ✘
#   conflicted = ✗
#   ahead = ⇡
#   behind = ⇣
#   diverged = ⇕
#   prompt_success = ➜
#   prompt_error = ➜
#   vimcmd = ⬅
#   vimcmd_replace_one = ⬅
#   vimcmd_replace = ⬅
#   vimcmd_visual = ⬅

# CURRENTLY USED SYMBOLS
#   arrow_right =   (U+E0B0)
#   semi-circle_right =   (U+E0B4)
#   top_line_connector = ╭─
#   bottom_line_connector = ╰─
#   git_branch =   (U+E725)
#   read_only = 󰌾  (U+F033E)
#   documents = 󰈙  (U+F0219)
#   downloads = 󰇚  (U+F01DA)
#   music = 󰝚  (U+F075A)
#   pictures =   (U+F03F1)
#   programming = 󰲋  (U+F0C8B)
#   stashed = 󰏖  (U+F03D6)

# GIT STATUS SYMBOLS
#   conflicted = ✗  |  ahead = ⇡  |  behind = ⇣  |  diverged = ⇕
#   untracked = ?   |  modified = !  |  staged = +
#   renamed = »     |  deleted = ✘

# CHARACTER/PROMPT SYMBOLS
#   success = ➜  |  error = ➜  |  vim = ⬅

# COLOR FLOW (Module → Next Module Background)
#   username (blue) → time (yellow)
#   time (yellow) → directory (maroon)
#   directory (maroon) → git_branch (teal)
#   git_branch (teal) → git_status (peach)
#   git_status (peach) → closes with peach arrow (dynamic!)
#   cmd_duration (pink) → closes with pink arrow (when shown >2s)

# ADDING NEW MODULES
# To insert a new module (e.g., nodejs, rust, python):
#
# 1. Add to format string in order:
#    format = """...$directory\n$nodejs\n$git_branch..."""
#
# 2. Update transition arrows:
#    [directory]
#    format = "[$path]($style)[](bg:green fg:maroon)"  # Point to nodejs
#
#    [nodejs]
#    symbol = " "
#    style = "bg:green fg:surface0"
#    format = '[$symbol($version)]($style)[](bg:teal fg:green)'  # Point to git_branch
#
# 3. Module maintains the chain: receives arrow from prev, sends arrow to next

# TIME FORMAT OPTIONS
# Current: "%H:%M" (24-hour, e.g., 14:30)
# Alternatives:
#   "%I:%M %p"  → 12-hour with AM/PM (2:30 PM)
#   "%H:%M:%S"  → 24-hour with seconds (14:30:45)
#   "%R"        → Same as %H:%M (shorthand)

# COMMON CUSTOMIZATIONS
# - Change truncation length: directory.truncation_length = 5
# - Disable time: time.disabled = true
# - Show hostname: add $hostname to format, configure [hostname]
# - Add battery: add $battery to format, configure [battery]
# - Change git symbols: modify git_status.<symbol> values
# - Swap to 12-hour time: time_format = "%I:%M %p"

# PERFORMANCE TIPS
# - Modules only execute when in format string
# - Parentheses () in format make module conditional (e.g., git_status)
# - min_time threshold prevents cmd_duration showing for fast commands
# - Use 'disabled = true' instead of removing from format (easier to toggle)
