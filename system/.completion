BREW_PREFIX=$(brew --prefix)

# Bash

<<<<<<< HEAD
if is-executable brew; then
  . "$BREW_PREFIX/share/bash-completion/bash_completion"
fi
||||||| parent of 6e38063 (personalized aliases with work and project traversal and removed problematic conditional in .completion)
if is-executable brew; then
  BREW_PREFIX=$(brew --prefix)
  BASH_COMPLETION="$BREW_PREFIX/share/bash-completion/bash_completion"
  . "$BASH_COMPLETION"
  unset BREW_PREFIX BASH_COMPLETION
fi
=======
# if is-executable brew; then
#   BREW_PREFIX=$(brew --prefix)
#   BASH_COMPLETION="$BREW_PREFIX/share/bash-completion/bash_completion"
#   . "$BASH_COMPLETION"
#   unset BREW_PREFIX BASH_COMPLETION
# fi
>>>>>>> 6e38063 (personalized aliases with work and project traversal and removed problematic conditional in .completion)

# Dotfiles

_dotfiles_completions() {
  local cur="${COMP_WORDS[COMP_CWORD]}"
  COMPREPLY=( $(compgen -W 'clean dock edit help macos test update' -- $cur ) );
}

complete -o default -F _dotfiles_completions dot

# npm (https://docs.npmjs.com/cli/completion)

if is-executable npm; then
  . <(npm completion)
fi

# fnm

if is-executable fnm; then
  . <(fnm completions)
fi

# Git

if is-executable git; then
  . "$BREW_PREFIX/etc/bash_completion.d/git-completion.bash"
fi
